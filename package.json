{
  "name": "music-pitch-changer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome Extension for real-time audio pitch shifting, BPM and key detection",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:postprocess",
    "build:worklets": "set VITE_ENTRY=pitch-processor&& vite build --config vite.worklets.config.ts && set VITE_ENTRY=bpm-processor&& vite build --config vite.worklets.config.ts && set VITE_ENTRY=key-processor&& vite build --config vite.worklets.config.ts",
    "build:all": "npm run build && npm run build:worklets",
    "build:postprocess": "if exist dist\\src (copy dist\\src\\popup.html dist\\popup.html && copy dist\\src\\offscreen.html dist\\offscreen.html && rmdir /S /Q dist\\src) && (if not exist dist\\manifest.json copy src\\manifest.json dist\\manifest.json) && (if not exist dist\\icons xcopy /E /I /Y public\\icons dist\\icons\\)",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/chrome": "^0.0.280",
    "@types/node": "^25.8.0",
    "@types/react": "^18.3.28",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "jsdom": "^25.0.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.16",
    "typescript": "^5.6.3",
    "vite": "^6.0.5",
    "vitest": "^2.1.0"
  }
}
